/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { ModifierSliderComponent } from './modifier-slider.component';



describe('ModifierSliderComponent', () => {
  let component: ModifierSliderComponent;
  let fixture: ComponentFixture<ModifierSliderComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ModifierSliderComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ModifierSliderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  /*let dataStorageService: DataSotrageService;
  beforeEach(inject([DataSotrageService], _dataStorageService => dataStorageService = _dataStorageService));*/

  /*it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/