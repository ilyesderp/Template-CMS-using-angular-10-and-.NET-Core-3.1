/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PopupElementsComponent } from './popup-elements.component';

describe('PopupElementsComponent', () => {
  let component: PopupElementsComponent;
  let fixture: ComponentFixture<PopupElementsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PopupElementsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PopupElementsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
